import 'package:flutter/material.dart';

class FavAppList extends StatelessWidget {
  const FavAppList({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
